\* Design as written: the orchestrator appends the ESCALATE entry before it acts, so before the user answers.
SPECIFICATION Spec
CONSTANTS
  Agents = {a1, a2}
  MaxQ = 2
  MaxCalls = 4
  MaxCrash = 1
  MaxOrchCalls = 1
  ReaskSettled = FALSE
  LogEscalatedBeforeUser = TRUE
INVARIANTS
  NoPendingEntry
CHECK_DEADLOCK FALSE
